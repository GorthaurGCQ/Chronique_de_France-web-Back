/**
 * Configuration globale Vitest — exécutée avant chaque fichier de test.
 * Fichier réservé aux hooks partagés (beforeAll, mocks globaux, polyfills…).
 * Actuellement vide : les mocks sont définis localement dans chaque suite.
 */
